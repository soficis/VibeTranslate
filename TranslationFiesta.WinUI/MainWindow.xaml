<Window
    x:Class="TranslationFiesta.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TranslationFiesta C# WinUI">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="TranslationFiesta C# WinUI"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,12" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Spacing="8"
                    Margin="0,0,0,8">
            <TextBlock Text="Provider:" VerticalAlignment="Center" />
            <ComboBox x:Name="ProviderCombo" Width="320" />
            <TextBlock x:Name="SourceCharCount"
                       Text="Ready"
                       VerticalAlignment="Center" />
        </StackPanel>

        <TextBox x:Name="SourceTextBox"
                 Grid.Row="2"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 PlaceholderText="Enter text to backtranslate..." />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    Spacing="8"
                    Margin="0,8,0,8">
            <Button x:Name="TranslateButton" Content="Backtranslate" />
            <Button x:Name="ImportButton" Content="Import" />
            <SplitButton x:Name="ExportButton" Content="Export">
                <SplitButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:Name="ExportPdfMenuItem" Text="Export as PDF" />
                        <MenuFlyoutItem x:Name="ExportDocxMenuItem" Text="Export as DOCX" />
                        <MenuFlyoutItem x:Name="ExportTxtMenuItem" Text="Export as Text" />
                    </MenuFlyout>
                </SplitButton.Flyout>
            </SplitButton>
            <Button x:Name="BatchButton" Content="Batch" />
        </StackPanel>

        <TextBox x:Name="TargetTextBox"
                 Grid.Row="4"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 PlaceholderText="Back-translation will appear here..." />

        <TextBlock x:Name="TargetCharCount"
                   Grid.Row="5"
                   Text="Target: 0 chars"
                   Margin="0,0,0,8" />

        <TextBox x:Name="PreviewTextBox"
                 Grid.Row="6"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 PlaceholderText="Preview..." />

        <!-- Compatibility controls used by code-behind -->
        <ComboBox x:Name="SourceLanguageCombo" Visibility="Collapsed" />
        <ComboBox x:Name="TargetLanguageCombo" Visibility="Collapsed" />
        <Button x:Name="SwapLanguagesButton" Visibility="Collapsed" />
        <Button x:Name="SourceCopyButton" Visibility="Collapsed" />
        <Button x:Name="TargetCopyButton" Visibility="Collapsed" />
        <Button x:Name="TargetSpeakButton" Visibility="Collapsed" />
        <Button x:Name="ClearButton" Visibility="Collapsed" />
        <Button x:Name="ManageTemplatesButton" Visibility="Collapsed" />
        <ComboBox x:Name="TemplateComboBox" Visibility="Collapsed" />
        <ComboBox x:Name="FormatComboBox" Visibility="Collapsed" />
        <Button x:Name="KeyboardShortcutsMenuItem" Visibility="Collapsed" />
    </Grid>
</Window>
